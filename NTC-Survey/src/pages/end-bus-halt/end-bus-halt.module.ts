import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EndBusHaltPage } from './end-bus-halt';

@NgModule({
  declarations: [
    EndBusHaltPage,
  ],
  imports: [
    IonicPageModule.forChild(EndBusHaltPage),
  ],
})
export class EndBusHaltPageModule {}
